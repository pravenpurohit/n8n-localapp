# Visual Test Baselines

Baseline screenshots for visual diff comparison. Generated by `npm run test:update-baselines`.

These are committed to git so baselines are shared. The self-healing test loop (Req 42) compares runtime screenshots against these baselines to detect visual regressions.
